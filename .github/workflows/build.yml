name: Build

on:
  workflow_call:
    inputs:
      environment:
        required: false
        type: string
        default: production
      worker_name_prefix:
        required: false
        type: string
        default: ''
        description: "Optional commit hash for versioned deployment (e.g., abc1234)"
      skip_build:
        required: false
        type: boolean
        default: false
        description: "Skip tests and build steps (requires build artifact from previous job)"
    outputs:
      url:
        description: "Deployment URL"
        value: ${{ jobs.build.outputs.deployment-url }}
      bundle-size:
        description: "Bundle size information"
        value: ${{ jobs.build.outputs.bundle-size }}
      duration:
        description: "Total duration"
        value: ${{ jobs.build.outputs.duration }}
      test-unit-time:
        description: "Unit tests duration"
        value: ${{ jobs.build.outputs.test-unit-time }}
      test-integration-time:
        description: "Integration tests duration"
        value: ${{ jobs.build.outputs.test-integration-time }}
      test-e2e-time:
        description: "E2E tests duration"
        value: ${{ jobs.build.outputs.test-e2e-time }}
      build-time:
        description: "Nuxt build duration"
        value: ${{ jobs.build.outputs.build-time }}
      deploy-time:
        description: "Cloudflare deploy duration"
        value: ${{ jobs.build.outputs.deploy-time }}
    secrets:
      CLOUDFLARE_ACCOUNT_ID:
        required: true
      CLOUDFLARE_API_TOKEN:
        required: true
      
jobs:
  build:
    name: "Build the application"
    runs-on: ubuntu-latest
    environment: ${{ inputs.environment }}
    outputs:
      deployment-url: ${{ steps.deploy.outputs.deployment-url }}
      bundle-size: ${{ steps.bundle-size.outputs.bundle-size }}
      duration: ${{ steps.calculate-duration.outputs.duration }}
      test-unit-time: ${{ steps.test-unit.outputs.duration }}
      test-integration-time: ${{ steps.test-integration.outputs.duration }}
      test-e2e-time: ${{ steps.test-e2e.outputs.duration }}
      build-time: ${{ steps.build-app.outputs.duration }}
      deploy-time: ${{ steps.deploy-app.outputs.duration }}

    permissions:
      id-token: write
      contents: read

    steps:
      - name: Record start time
        id: start-time
        run: echo "start-time=$(date +%s)" >> $GITHUB_OUTPUT

      - name: Checkout
        uses: actions/checkout@v4
        with:
          # Fetch full history for accurate affected test detection on PRs
          fetch-depth: 0

      - name: Setup PNPM
        uses: pnpm/action-setup@v4
        with:
          run_install: false

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: 'pnpm'

      - name: Install dependencies
        if: ${{ !inputs.skip_build }}
        run: pnpm install --frozen-lockfile

      - name: Run ESLint
        if: ${{ !inputs.skip_build }}
        run: pnpm run lint

      - name: Run TypeScript type checks
        if: ${{ !inputs.skip_build }}
        run: |
          pnpm run dev:prepare
          pnpm run typecheck

      - name: Run unit tests (PR - affected only)
        if: ${{ !inputs.skip_build && github.event_name == 'pull_request' }}
        id: test-unit-pr
        run: |
          START=$(date +%s)
          node scripts/test-affected.mjs --base=origin/${{ github.base_ref }} --type=unit
          END=$(date +%s)
          DURATION=$((END - START))
          echo "duration=${DURATION}s" >> $GITHUB_OUTPUT
          echo "✅ Affected unit tests completed in ${DURATION}s"

      - name: Run all unit tests (main)
        if: ${{ !inputs.skip_build && github.event_name != 'pull_request' }}
        id: test-unit-main
        run: |
          START=$(date +%s)
          pnpm run test:unit
          END=$(date +%s)
          DURATION=$((END - START))
          echo "duration=${DURATION}s" >> $GITHUB_OUTPUT
          echo "✅ All unit tests completed in ${DURATION}s"

      - name: Set unit test duration output
        id: test-unit
        run: |
          if [ "${{ inputs.skip_build }}" == "true" ]; then
            echo "duration=skipped" >> $GITHUB_OUTPUT
          elif [ "${{ github.event_name }}" == "pull_request" ]; then
            echo "duration=${{ steps.test-unit-pr.outputs.duration }}" >> $GITHUB_OUTPUT
          else
            echo "duration=${{ steps.test-unit-main.outputs.duration }}" >> $GITHUB_OUTPUT
          fi

      - name: Run integration tests (PR - affected only)
        if: ${{ !inputs.skip_build && github.event_name == 'pull_request' }}
        id: test-integration-pr
        run: |
          START=$(date +%s)
          node scripts/test-affected.mjs --base=origin/${{ github.base_ref }} --type=integration
          END=$(date +%s)
          DURATION=$((END - START))
          echo "duration=${DURATION}s" >> $GITHUB_OUTPUT
          echo "✅ Affected integration tests completed in ${DURATION}s"

      - name: Run all integration tests (main)
        if: ${{ !inputs.skip_build && github.event_name != 'pull_request' }}
        id: test-integration-main
        run: |
          START=$(date +%s)
          pnpm run test:integration
          END=$(date +%s)
          DURATION=$((END - START))
          echo "duration=${DURATION}s" >> $GITHUB_OUTPUT
          echo "✅ All integration tests completed in ${DURATION}s"

      - name: Set integration test duration output
        id: test-integration
        run: |
          if [ "${{ inputs.skip_build }}" == "true" ]; then
            echo "duration=skipped" >> $GITHUB_OUTPUT
          elif [ "${{ github.event_name }}" == "pull_request" ]; then
            echo "duration=${{ steps.test-integration-pr.outputs.duration }}" >> $GITHUB_OUTPUT
          else
            echo "duration=${{ steps.test-integration-main.outputs.duration }}" >> $GITHUB_OUTPUT
          fi

      - name: Install Playwright browsers
        if: ${{ !inputs.skip_build }}
        run: pnpm exec playwright install --with-deps

      - name: Run E2E tests (PR - affected only)
        if: ${{ !inputs.skip_build && github.event_name == 'pull_request' }}
        id: test-e2e-pr
        run: |
          START=$(date +%s)
          node scripts/test-affected.mjs --base=origin/${{ github.base_ref }} --type=e2e
          END=$(date +%s)
          DURATION=$((END - START))
          echo "duration=${DURATION}s" >> $GITHUB_OUTPUT
          echo "✅ Affected E2E tests completed in ${DURATION}s"
        env:
          CI: true
          #DEBUG: pw:webserver
          #NODE_OPTIONS: --trace-warnings

      - name: Run all E2E tests (main)
        if: ${{ !inputs.skip_build && github.event_name != 'pull_request' }}
        id: test-e2e-main
        run: |
          START=$(date +%s)
          pnpm run test:e2e
          END=$(date +%s)
          DURATION=$((END - START))
          echo "duration=${DURATION}s" >> $GITHUB_OUTPUT
          echo "✅ All E2E tests completed in ${DURATION}s"
        env:
          CI: true
          #DEBUG: pw:webserver
          #NODE_OPTIONS: --trace-warnings

      - name: Set E2E test duration output
        id: test-e2e
        run: |
          if [ "${{ inputs.skip_build }}" == "true" ]; then
            echo "duration=skipped" >> $GITHUB_OUTPUT
          elif [ "${{ github.event_name }}" == "pull_request" ]; then
            echo "duration=${{ steps.test-e2e-pr.outputs.duration }}" >> $GITHUB_OUTPUT
          else
            echo "duration=${{ steps.test-e2e-main.outputs.duration }}" >> $GITHUB_OUTPUT
          fi

      - name: Upload Playwright report
        uses: actions/upload-artifact@v4
        if: always() && (steps.test-e2e-pr.outcome != 'skipped' || steps.test-e2e-main.outcome != 'skipped')
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30

      - name: Build application
        if: ${{ !inputs.skip_build }}
        id: build-app-run
        env:
          ROLLDOWN_OPTIONS_VALIDATION: loose
        run: |
          START=$(date +%s)
          pnpm run build 2>&1 | tee build-output.log
          END=$(date +%s)
          DURATION=$((END - START))
          echo "duration=${DURATION}s" >> $GITHUB_OUTPUT
          echo "✅ Build completed in ${DURATION}s"

      - name: Set build time output
        id: build-app
        run: |
          if [ "${{ inputs.skip_build }}" == "true" ]; then
            echo "duration=skipped" >> $GITHUB_OUTPUT
          else
            echo "duration=${{ steps.build-app-run.outputs.duration }}" >> $GITHUB_OUTPUT
          fi

      - name: Extract and output bundle size
        if: ${{ !inputs.skip_build }}
        id: bundle-size-extract
        run: |
          SIZE_INFO=$(grep -E "Total size:.*MB.*gzip" build-output.log | sed 's/\x1b\[[0-9;]*m//g' | sed 's/Σ Total size: //' || echo "Size information not found")
          echo "Bundle Size: $SIZE_INFO"
          echo "bundle-size=$SIZE_INFO" >> $GITHUB_OUTPUT

      - name: Set bundle size output
        id: bundle-size
        run: |
          if [ "${{ inputs.skip_build }}" == "true" ]; then
            echo "bundle-size=reused from previous build" >> $GITHUB_OUTPUT
          else
            echo "bundle-size=${{ steps.bundle-size-extract.outputs.bundle-size }}" >> $GITHUB_OUTPUT
          fi

      - name: Verify build output exists
        if: ${{ !inputs.skip_build }}
        run: |
          echo "Checking if .output/ directory exists..."
          if [ ! -d ".output" ]; then
            echo "❌ ERROR: .output/ directory not found!"
            echo "Build may have failed silently."
            echo ""
            echo "Current directory contents:"
            ls -la
            echo ""
            echo "Checking for common build output locations:"
            ls -la dist/ 2>/dev/null || echo "No dist/ directory"
            ls -la build/ 2>/dev/null || echo "No build/ directory"
            exit 1
          fi

          echo "✅ .output/ directory exists"
          echo ""
          echo "Contents of .output/:"
          ls -lah .output/
          echo ""
          echo "Disk usage:"
          du -sh .output/

      - name: Upload build output
        if: ${{ !inputs.skip_build }}
        uses: actions/upload-artifact@v4
        with:
          name: build-output-${{ github.run_id }}
          path: .output/
          retention-days: 1
          if-no-files-found: error
          include-hidden-files: true

      - name: Download build output
        if: ${{ inputs.skip_build }}
        uses: actions/download-artifact@v4
        with:
          name: build-output-${{ github.run_id }}
          path: .output/

      - name: Record deploy start time
        id: deploy-start
        run: echo "start-time=$(date +%s)" >> $GITHUB_OUTPUT

      - name: Prepare deployment command
        id: deploy-cmd
        run: |
          if [ -n "${{ inputs.worker_name_prefix }}" ]; then
            SHORT_HASH=$(echo '${{ inputs.worker_name_prefix }}' | cut -c1-7)
            echo "command=deploy --name ${SHORT_HASH}-besidka-preview" >> $GITHUB_OUTPUT
          elif [ "${{ inputs.environment }}" == "production" ]; then
            echo "command=deploy --env production" >> $GITHUB_OUTPUT
          else
            echo "command=deploy" >> $GITHUB_OUTPUT
          fi

      - name: Deploy app to Cloudflare Workers via Wrangler
        id: deploy
        uses: cloudflare/wrangler-action@v3
        with:
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          packageManager: pnpm
          command: ${{ steps.deploy-cmd.outputs.command }}

      - name: Calculate deploy time
        id: deploy-app
        run: |
          START=${{ steps.deploy-start.outputs.start-time }}
          END=$(date +%s)
          DURATION=$((END - START))
          echo "duration=${DURATION}s" >> $GITHUB_OUTPUT
          echo "✅ Deploy completed in ${DURATION}s"

      - name: Print deployed URL
        env:
          DEPLOYMENT_URL: ${{ steps.deploy.outputs.deployment-url }}
        run: echo $DEPLOYMENT_URL

      - name: Calculate total duration and print summary
        id: calculate-duration
        run: |
          START_TIME=${{ steps.start-time.outputs.start-time }}
          END_TIME=$(date +%s)
          TOTAL_DURATION=$((END_TIME - START_TIME))
          MINUTES=$((TOTAL_DURATION / 60))
          SECONDS=$((TOTAL_DURATION % 60))
          if [ $MINUTES -gt 0 ]; then
            DURATION_FORMAT="${MINUTES}m ${SECONDS}s"
          else
            DURATION_FORMAT="${SECONDS}s"
          fi
          echo "duration=$DURATION_FORMAT" >> $GITHUB_OUTPUT

          # Print beautiful summary
          echo ""
          echo "╔════════════════════════════════════════════════════════════╗"
          echo "║                    BUILD SUMMARY                           ║"
          echo "╠════════════════════════════════════════════════════════════╣"
          echo "║                                                            ║"
          echo "║  📊 Test Results:                                          ║"
          echo "║     • Unit tests:        ${{ steps.test-unit.outputs.duration }}" | awk '{printf "%-60s║\n", $0}'
          echo "║     • Integration tests: ${{ steps.test-integration.outputs.duration }}" | awk '{printf "%-60s║\n", $0}'
          echo "║     • E2E tests:         ${{ steps.test-e2e.outputs.duration }}" | awk '{printf "%-60s║\n", $0}'
          echo "║                                                            ║"
          echo "║  🏗️  Build Process:                                        ║"
          echo "║     • Nuxt build:        ${{ steps.build-app.outputs.duration }}" | awk '{printf "%-60s║\n", $0}'
          echo "║     • Bundle size:       ${{ steps.bundle-size.outputs.bundle-size }}" | awk '{printf "%-60s║\n", $0}'
          echo "║                                                            ║"
          echo "║  🚀 Deployment:                                            ║"
          echo "║     • Deploy time:       ${{ steps.deploy-app.outputs.duration }}" | awk '{printf "%-60s║\n", $0}'
          echo "║     • URL:               ${{ steps.deploy.outputs.deployment-url }}" | awk '{printf "%-60s║\n", $0}' | head -c 66
          echo "║                                                            ║"
          echo "║  ⏱️  Total Time:          ${DURATION_FORMAT}" | awk '{printf "%-60s║\n", $0}'
          echo "║                                                            ║"
          echo "╚════════════════════════════════════════════════════════════╝"
          echo ""