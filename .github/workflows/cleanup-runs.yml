name: Cleanup workflow runs

on:
  schedule:
    # Once a week, Sundays at 03:23 UTC
    - cron: '23 3 * * 0'
  workflow_dispatch:

concurrency:
  group: cleanup-workflow-runs
  cancel-in-progress: true

jobs:
  cleanup:
    name: Delete skipped and matched runs
    runs-on: ubuntu-24.04
    permissions:
      actions: write
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Clean workflow runs
        env:
          GH_TOKEN: ${{ github.token }}
        run: ./scripts/clean-gh-runs.sh --yes --limit 500
